ShopEase — Demo Frontend UI

How to open:
1. Open the folder `ui` in your editor or file explorer.
2. Open `index.html` in a browser (double-click or use Live Server).

What it includes:
- Responsive product grid with sample products
- Product modal for details
- Simple client-side cart with quantity controls and a mock checkout flow

Notes:
- Images are loaded from Unsplash (requires internet).
- This is a static demo UI intended as a starting point for integration with an API.

Enjoy!